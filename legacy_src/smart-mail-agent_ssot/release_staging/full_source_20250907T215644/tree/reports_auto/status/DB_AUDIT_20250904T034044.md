# DB Audit 20250904T034044
- db_path: `reports_auto/sma.sqlite3`
- ingested_actions: 10
- distribution: {"done": 10, "error": 0, "queued": 0}

## Schema
### INDEX `idx_actions_status`
```
CREATE INDEX idx_actions_status ON actions(status)
```

### INDEX `idx_dead_letters_when`
```
CREATE INDEX idx_dead_letters_when ON dead_letters(created_at)
```

### INDEX `idx_orders_po`
```
CREATE INDEX idx_orders_po ON orders(po_no)
```

### INDEX `idx_shipments_tracking`
```
CREATE INDEX idx_shipments_tracking ON shipments(tracking_no)
```

### INDEX `ux_actions_idem`
```
CREATE UNIQUE INDEX ux_actions_idem ON actions(idem)
```

### INDEX `ux_alerts_idem`
```
CREATE UNIQUE INDEX ux_alerts_idem ON alerts(idem)
```

### INDEX `ux_answers_idem`
```
CREATE UNIQUE INDEX ux_answers_idem ON answers(idem)
```

### INDEX `ux_changes_idem`
```
CREATE UNIQUE INDEX ux_changes_idem ON changes(idem)
```

### INDEX `ux_errors_idem`
```
CREATE UNIQUE INDEX ux_errors_idem ON errors(idem)
```

### INDEX `ux_mails_idem`
```
CREATE UNIQUE INDEX ux_mails_idem ON mails(idem)
```

### INDEX `ux_quotes_idem`
```
CREATE UNIQUE INDEX ux_quotes_idem ON quotes(idem)
```

### INDEX `ux_tickets_idem`
```
CREATE UNIQUE INDEX ux_tickets_idem ON tickets(idem)
```

### TABLE `actions`
```
CREATE TABLE actions(
  id INTEGER PRIMARY KEY,
  idem TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'queued',
  payload TEXT,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now'))
)
```

### TABLE `actions_history`
```
CREATE TABLE actions_history(
  id INTEGER PRIMARY KEY,
  action_id INTEGER NOT NULL,
  old_status TEXT,
  new_status TEXT,
  changed_at TEXT NOT NULL DEFAULT (datetime('now')),
  FOREIGN KEY(action_id) REFERENCES actions(id)
)
```

### TABLE `alerts`
```
CREATE TABLE alerts(
  id INTEGER PRIMARY KEY,
  idem TEXT NOT NULL,
  level TEXT, message TEXT, meta TEXT,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
)
```

### TABLE `answers`
```
CREATE TABLE answers(
  id INTEGER PRIMARY KEY,
  idem TEXT NOT NULL,
  action_id INTEGER, content TEXT,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  FOREIGN KEY(action_id) REFERENCES actions(id)
)
```

### TABLE `changes`
```
CREATE TABLE changes(
  id INTEGER PRIMARY KEY,
  idem TEXT NOT NULL,
  obj TEXT, before TEXT, after TEXT,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
)
```

### TABLE `dead_letters`
```
CREATE TABLE dead_letters(
  id INTEGER PRIMARY KEY, idem TEXT NOT NULL, payload TEXT, reason TEXT, retry_after TEXT,
  created_at TEXT NOT NULL DEFAULT (datetime('now')) )
```

### TABLE `errors`
```
CREATE TABLE errors(
  id INTEGER PRIMARY KEY,
  idem TEXT NOT NULL,
  phase TEXT, message TEXT, trace TEXT,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
)
```

### TABLE `invoices`
```
CREATE TABLE invoices(
  id INTEGER PRIMARY KEY, idem TEXT NOT NULL, title TEXT, vat TEXT, amount TEXT, status TEXT,
  created_at TEXT NOT NULL DEFAULT (datetime('now')) )
```

### TABLE `mails`
```
CREATE TABLE mails(
  id INTEGER PRIMARY KEY,
  idem TEXT NOT NULL,
  subject TEXT, sender TEXT, body TEXT,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
)
```

### TABLE `orders`
```
CREATE TABLE orders(
  id INTEGER PRIMARY KEY, idem TEXT NOT NULL, po_no TEXT, items_json TEXT, amount TEXT, currency TEXT,
  created_at TEXT NOT NULL DEFAULT (datetime('now')) )
```

### TABLE `quotes`
```
CREATE TABLE quotes(
  id INTEGER PRIMARY KEY,
  idem TEXT NOT NULL,
  action_id INTEGER, amount TEXT, currency TEXT, extra TEXT,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  FOREIGN KEY(action_id) REFERENCES actions(id)
)
```

### TABLE `rmas`
```
CREATE TABLE rmas(
  id INTEGER PRIMARY KEY, idem TEXT NOT NULL, rma_no TEXT, sn TEXT, reason TEXT,
  created_at TEXT NOT NULL DEFAULT (datetime('now')) )
```

### TABLE `shipments`
```
CREATE TABLE shipments(
  id INTEGER PRIMARY KEY, idem TEXT NOT NULL, tracking_no TEXT, carrier TEXT, items_json TEXT,
  created_at TEXT NOT NULL DEFAULT (datetime('now')) )
```

### TABLE `tickets`
```
CREATE TABLE tickets(
  id INTEGER PRIMARY KEY,
  idem TEXT NOT NULL,
  action_id INTEGER, data TEXT,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  FOREIGN KEY(action_id) REFERENCES actions(id)
)
```

### TRIGGER `trg_actions_status`
```
CREATE TRIGGER trg_actions_status
AFTER UPDATE OF status ON actions
FOR EACH ROW
WHEN OLD.status IS NOT NEW.status
BEGIN
  INSERT INTO actions_history(action_id, old_status, new_status, changed_at)
  VALUES (OLD.id, OLD.status, NEW.status, datetime('now'));
  UPDATE actions SET updated_at = datetime('now') WHERE id = NEW.id;
END
```

### VIEW `v_actions_summary`
```
CREATE VIEW v_actions_summary AS
SELECT json_extract(a.payload, '$.intent.intent') AS intent,
       COUNT(*) AS cnt
FROM actions a GROUP BY intent
```

### VIEW `v_answers`
```
CREATE VIEW v_answers AS
SELECT ans.id, ans.idem, ans.created_at, a.status,
       substr(ans.content, 1, 200) AS preview
FROM answers ans LEFT JOIN actions a ON a.id = ans.action_id
```

### VIEW `v_quality_gaps`
```
CREATE VIEW v_quality_gaps AS
SELECT json_extract(a.payload, '$.intent.intent') AS intent,
       AVG(CASE WHEN json_extract(a.payload, '$.kie.coverage.amount')=1 THEN 1 ELSE 0 END) AS cov_amount,
       AVG(CASE WHEN json_extract(a.payload, '$.kie.coverage.vat')=1 THEN 1 ELSE 0 END) AS cov_vat
FROM actions a GROUP BY intent
```

### VIEW `v_tickets`
```
CREATE VIEW v_tickets AS
SELECT t.id, t.idem, t.created_at, a.status,
       json_extract(t.data,'$.title') AS title,
       json_extract(t.data,'$.amount') AS amount
FROM tickets t LEFT JOIN actions a ON a.id = t.action_id
```
