lambda url: [{"tag": "h1", "text": "合作"}])
    # 分類回空集合
    monkeypatch.setattr(
        fc, "classify_dir", lambda p: {"image": [], "pdf": [], "text": [], "other": []}
    )

    argv = [
        "prog",
        "--tasks",
        "ocr,scrape,classify_files,nlp",
        "--input-path",
        str(tmp_path),
        "--url",
        "http://stub.local",
        "--dry-run",
    ]
    monkeypatch.setattr(sys, "argv", argv)
    rc = main()
    assert rc == 0

```

### legacy_tests/ai_rpa/test_nlp.py
```python
from ai_rpa.nlp import analyze_text


def test_analyze_text_offline():
    out = analyze_text(["我要退款", "想合作直播"], model="offline-keyword")
    assert out["labels"] == ["refund", "sales"]

```

### legacy_tests/ai_rpa/test_nlp_fallback.py
```python
from ai_rpa.nlp import analyze_text


def test_analyze_text_fallback_to_offline():
    out = analyze_text(["我要退款"], model="transformers")
    # 會經過 warning 分支後回到 offline-keyword
    assert out["labels"] == ["refund"]

```

### legacy_tests/ai_rpa/test_ocr.py
```python
import sys
import types

from ai_rpa.ocr import run_ocr


def test_ocr_missing_file(tmp_path):
    out = run_ocr(str(tmp_path / "no.png"))
    assert out["text"] == ""


def test_ocr_with_fake_engine(tmp_path, monkeypatch):
    # 建立測試影像（Pillow 由 requirements 保證存在）
    from PIL import Image, ImageDraw

    p = tmp_path / "img.png"
    im = Image.new("RGB", (64, 32), "white")
    d = ImageDraw.Draw(im)
    d.text((2, 2), "OK", fill="black")
    im.save(p)

    # 注入假的 pytesseract
    fake = types.SimpleNamespace(image_to_string=lambda im: "OK")
    monkeypatch.setitem(sys.modules, "pytesseract", fake)
    out = run_ocr(str(p))
    assert out["text"] == "OK"

```

### legacy_tests/ai_rpa/test_ocr_edges.py
```python
import builtins

from ai_rpa import ocr as ocr_mod


def test_ocr_no_pillow(monkeypatch, tmp_path):
    orig_import = builtins.__import__

    def fake_import(name, *a, **k):
        if name == "PIL" or name.startswith("PIL."):
            raise ImportError("no PIL")
        return orig_import(name, *a, **k)

    monkeypatch.setattr(builtins, "__import__", fake_import)
    out = ocr_mod.run_ocr(str(tmp_path / "x.png"))
    assert out["text"] == ""


def test_ocr_no_pytesseract_with_image(monkeypatch, tmp_path):
    # 產生可開啟的影像
    from PIL import Image

    p = tmp_path / "ok.png"
    Image.new("RGB", (8, 8), "white").save(p)
    # 禁用 pytesseract 匯入
    orig_import = builtins.__import__

    def fake_import(name, *a, **k):
        if name == "pytesseract" or name.startswith("pytesseract."):
            raise ImportError("no pytesseract")
        return orig_import(name, *a, **k)

    monkeypatch.setattr(builtins, "__import__", fake_import)
    out = ocr_mod.run_ocr(str(p))
    assert out["text"] == ""  # 走到 pytesseract None 的退化分支


def test_ocr_image_open_failure(monkeypatch, tmp_path):
    # 確保有 PIL，但令 Image.open 拋例外
    from PIL import Image

    def boom(*a, **k):
        raise RuntimeError("open failed")

    monkeypatch.setattr(Image, "open", boom)
    out = ocr_mod.run_ocr(str(tmp_path / "x.png"))
    assert out["text"] == ""  # 命中最後的 except 分支

```

### legacy_tests/ai_rpa/test_ocr_raise_in_engine.py
```python
import sys
import types

from PIL import Image

from ai_rpa.ocr import run_ocr


def test_ocr_image_to_string_raises(tmp_path, monkeypatch):
    p = tmp_path / "img.png"
    Image.new("RGB", (8, 8), "white").save(p)

    # 構造 pytesseract 並讓其 image_to_string 拋出例外
    fake = types.SimpleNamespace(
        image_to_string=lambda im: (_ for _ in ()).throw(RuntimeError("boom"))
    )
    monkeypatch.setitem(sys.modules, "pytesseract", fake)

    out = run_ocr(str(p))
    # 例外應被捕捉並回傳空字串（覆蓋最後 except）
    assert out["text"] == ""

```

### legacy_tests/ai_rpa/test_scraper.py
```python
from ai_rpa.scraper import scrape


class DummyResp:
    def __init__(self, text):
        self.text = text

    def raise_for_status(self):
        pass


def test_scrape_monkeypatch(monkeypatch):
    html = "<html><h1>T1</h1><h2>T2</h2><p>x</p></html>"
    monkeypatch.setattr("requests.get", lambda url, timeout=10: DummyResp(html))
    out = scrape("http://x")
    assert {"tag": "h1", "text": "T1"} in out and {"tag": "h2", "text": "T2"} in out

```

### legacy_tests/ai_rpa/test_scraper_empty_headings.py
```python
from ai_rpa.scraper import scrape


class DummyResp:
    def __init__(self, text):
        self.text = text

    def raise_for_status(self):
        pass


def test_scraper_empty_headings(monkeypatch):
    html = "<html><h1>   </h1><h2></h2><p>x</p></html>"
    monkeypatch.setattr("requests.get", lambda url, timeout=10: DummyResp(html))
    out = scrape("http://x")
    assert out == []

```

### legacy_tests/internal_smoke/test_import_all_internal.py
```python
import importlib
import pkgutil

import pytest

import smart_mail_agent

# 自動發現 smart_mail_agent 下面的所有可匯入模組（避免手寫重覆清單）
mods = [
    m.name for m in pkgutil.walk_packages(smart_mail_agent.__path__, prefix="smart_mail_agent.")
]


@pytest.mark.parametrize("mod", mods)
def test_import_module(mod: str) -> None:
    importlib.import_module(mod)

```

### mkdocs.yml
```yaml
site_name: smart-mail-agent
theme:
name: material
nav:

Home: index.md

CLI: cli.md

Development: dev.md

```

### models/__init__.py
```python
# models root package

```

### models/common/__init__.py
```python
from .jsonl import read_jsonl, write_jsonl
from .text import tokenize, norm

```

### models/common/jsonl.py
```python
from __future__ import annotations
from pathlib import Path
import json

def read_jsonl(path):
    p = Path(path)
    if not p.exists(): return
    with p.open("r", encoding="utf-8") as f:
        for line in f:
            line = line.strip()
            if not line: continue
            yield json.loads(line)

def write_jsonl(path, rows):
    p = Path(path); p.parent.mkdir(parents=True, exist_ok=True)
    with p.open("w", encoding="utf-8") as f:
        for r in rows:
            f.write(json.dumps(r, ensure_ascii=False) + "\n")

```

### models/common/text.py
```python
from __future__ import annotations
import re

_ws = re.compile(r"\s+", re.U)
_tok = re.compile(r"[0-9A-Za-z\u4e00-\u9fff]+", re.U)

def norm(t: str) -> str:
    return _ws.sub(" ", (t or "").strip())

def tokenize(t: str):
    return _tok.findall((t or "").lower())

```

### models/intent/__init__.py
```python
# 讓 `from models.intent import train as intent_train` 取得的是子模組
from . import train as train
from .train import load, predict  # 函式另外提供
__all__ = ["train","load","predict"]

```

### models/intent/data/README.md
```md
# Intent 標註說明（JSONL）
每行一筆：
{"text": "<郵件或句子>", "label": "<biz_quote|tech_support|policy_qa|profile_update|complaint|other>"}

建議拆 train/val/test 三份，先從 100~500 筆起步，逐步擴充。
標註一致性很關鍵：可寫 10~20 條「判斷規則」給標註者參考。

```

### models/intent/data/test.jsonl
```
{"text": "請提供正式報價與價格明細", "label": "biz_quote"}
{"text": "體驗很糟糕", "label": "complaint"}
{"text": "重設密碼失敗", "label": "tech_support"}
{"text": "請更新地址與電話", "label": "profile_update"}
{"text": "無法登入需要協助", "label": "tech_support"}
{"text": "合約條款與限制", "label": "policy_qa"}
{"text": "退款流程怎麼申請", "label": "policy_qa"}
{"text": "合約條款與限制", "label": "policy_qa"}
{"text": "功能異常 麻煩支援", "label": "tech_support"}
{"text": "收到", "label": "other"}
{"text": "物流延遲多日 非常不滿", "label": "complaint"}
{"text": "請更新地址與電話", "label": "profile_update"}
{"text": "請更新地址與電話", "label": "profile_update"}
{"text": "對服務品質感到失望", "label": "complaint"}
{"text": "可否安排方案與費用", "label": "biz_quote"}
{"text": "收到", "label": "other"}
{"text": "嗨", "label": "other"}
{"text": "可否安排方案與費用", "label": "biz_quote"}
```

### models/intent/data/train.jsonl
```
{"text": "收到", "label": "other"}
{"text": "想洽談合作並詢問報價", "label": "biz_quote"}
{"text": "修改聯絡 email", "label": "profile_update"}
{"text": "無法登入需要協助", "label": "tech_support"}
{"text": "合約條款與限制", "label": "policy_qa"}
{"text": "變更發票抬頭資訊", "label": "profile_update"}
{"text": "請提供正式報價與價格明細", "label": "biz_quote"}
{"text": "對服務品質感到失望", "label": "complaint"}
{"text": "想洽談合作並詢問報價", "label": "biz_quote"}
{"text": "對服務品質感到失望", "label": "complaint"}
{"text": "謝謝", "label": "other"}
{"text": "物流延遲多日 非常不滿", "label": "complaint"}
{"text": "重設密碼失敗", "label": "tech_support"}
{"text": "對服務品質感到失望", "label": "complaint"}
{"text": "請提供正式報價與價格明細", "label": "biz_quote"}
{"text": "想洽談合作並詢問報價", "label": "biz_quote"}
{"text": "謝謝", "label": "other"}
{"text": "收到", "label": "other"}
{"text": "很失望 客服回覆太慢", "label": "complaint"}
{"text": "體驗很糟糕", "label": "complaint"}
{"text": "報價單可否提供最新版", "label": "biz_quote"}
{"text": "退款流程怎麼申請", "label": "policy_qa"}
{"text": "請更新地址與電話", "label": "profile_update"}
{"text": "謝謝", "label": "other"}
{"text": "可否安排方案與費用", "label": "biz_quote"}
{"text": "功能異常 麻煩支援", "label": "tech_support"}
{"text": "收到", "label": "other"}
{"text": "退款流程怎麼申請", "label": "policy_qa"}
{"text": "試用方案限制有哪些", "label": "policy_qa"}
{"text": "功能異常 麻煩支援", "label": "tech_support"}
{"text": "重設密碼失敗", "label": "tech_support"}
{"text": "報價單可否提供最新版", "label": "biz_quote"}
{"text": "請更新地址與電話", "label": "profile_update"}
{"text": "嗨", "label": "other"}
{"text": "合約條款與限制", "label": "policy_qa"}
{"text": "請提供正式報價與價格明細", "label": "biz_quote"}
{"text": "功能異常 麻煩支援", "label": "tech_support"}
{"text": "報價單可否提供最新版", "label": "biz_quote"}
{"text": "物流延遲多日 非常不滿", "label": "complaint"}
{"text": "請提供正式報價與價格明細", "label": "biz_quote"}
{"text": "試用方案限制有哪些", "label": "policy_qa"}
{"text": "你好 想了解更多", "label": "other"}
{"text": "報價單可否提供最新版", "label": "biz_quote"}
{"text": "體驗很糟糕", "label": "complaint"}
{"text": "對服務品質感到失望", "label": "complaint"}
{"text": "報價單可否提供最新版", "label": "biz_quote"}
{"text": "變更發票抬頭資訊", "label": "profile_update"}
{"text": "請更新地址與電話", "label": "profile_update"}
{"text": "對服務品質感到失望", "label": "complaint"}
{"text": "重設密碼失敗", "label": "tech_support"}
{"text": "合約條款與限制", "label": "policy_qa"}
{"text": "系統錯誤請協助", "label": "tech_support"}
{"text": "想了解退款機制與使用限制", "label": "policy_qa"}
{"text": "請更新地址與電話", "label": "profile_update"}
{"text": "想洽談合作並詢問報價", "label": "biz_quote"}
{"text": "重設密碼失敗", "label": "tech_support"}
{"text": "你好 想了解更多", "label": "other"}
{"text": "合約條款與限制", "label": "policy_qa"}
{"text": "更新客戶資料", "label": "profile_update"}
{"text": "變更發票抬頭資訊", "label": "profile_update"}
{"text": "可否安排方案與費用", "label": "biz_quote"}
{"text": "謝謝", "label": "other"}
{"text": "無法登入需要協助", "label": "tech_support"}
{"text": "修改聯絡 email", "label": "profile_update"}
{"text": "退款流程怎麼申請", "label": "policy_qa"}
{"text": "你好 想了解更多", "label": "other"}
{"text": "合約條款與限制", "label": "policy_qa"}
{"text": "很失望 客服回覆太慢", "label": "complaint"}
{"text": "無法登入需要協助", "label": "tech_support"}
{"text": "變更發票抬頭資訊", "label": "profile_update"}
{"text": "很失望 客服回覆太慢", "label": "complaint"}
{"text": "退款流程怎麼申請", "label": "policy_qa"}
{"text": "請更新地址與電話", "label": "profile_update"}
{"text": "功能異常 麻煩支援", "label": "tech_support"}
{"text": "你好 想了解更多", "label": "other"}
{"text": "功能異常 麻煩支援", "label": "tech_support"}
{"text": "想了解退款機制與使用限制", "label": "policy_qa"}
{"text": "謝謝", "label": "other"}
{"text": "重設密碼失敗", "label": "tech_support"}
{"text": "更新客戶資料", "label": "profile_update"}
{"text": "合約條款與限制", "label": "policy_qa"}
{"text": "請提供正式報價與價格明細", "label": "biz_quote"}
{"text": "謝謝", "label": "other"}
{"text": "想了解退款機制與使用限制", "label": "policy_qa"}
{"text": "收到", "label": "other"}
{"text": "重設密碼失敗", "label": "tech_support"}
{"text": "很失望 客服回覆太慢", "label": "complaint"}
{"text": "無法登入需要協助", "label": "tech_support"}
{"text": "修改聯絡 email", "label": "profile_update"}
{"text": "系統錯誤請協助", "label": "tech_support"}
{"text": "想洽談合作並詢問報價", "label": "biz_quote"}
{"text": "想洽談合作並詢問報價", "label": "biz_quote"}
{"text": "謝謝", "label": "other"}
{"text": "謝謝", "label": "other"}
{"text": "謝謝", "label": "other"}
{"text": "報價單可否提供最新版", "label": "biz_quote"}
{"text": "很失望 客服回覆太慢", "label": "complaint"}
{"text": "很失望 客服回覆太慢", "label": "complaint"}
{"text": "想了解退款機制與使用限制", "label": "policy_qa"}
{"text": "變更發票抬頭資訊", "label": "profile_update"}
{"text": "更新客戶資料", "label": "profile_update"}
{"text": "功能異常 麻煩支援", "label": "tech_support"}
{"text": "很失望 客服回覆太慢", "label": "complaint"}
{"text": "體驗很糟糕", "label": "complaint"}
{"text": "功能異常 麻煩支援", "label": "tech_support"}
{"text": "謝謝", "label": "other"}
{"text": "試用方案限制有哪些", "label": "policy_qa"}
{"text": "報價單可否提供最新版", "label": "biz_quote"}
{"text": "想洽談合作並詢問報價", "label": "biz_quote"}
{"text": "功能異常 麻煩支援", "label": "tech_support"}
{"text": "請提供正式報價與價格明細", "label": "biz_quote"}
{"text": "謝謝", "label": "other"}
{"text": "無法登入需要協助", "label": "tech_support"}
{"text": "請提供正式報價與價格明細", "label": "biz_quote"}
{"text": "退款流程怎麼申請", "label": "policy_qa"}
{"text": "修改聯絡 email", "label": "profile_update"}
{"text": "收到", "label": "other"}
{"text": "你好 想了解更多", "label": "other"}
{"text": "合約條款與限制", "label": "policy_qa"}
{"text": "修改聯絡 email", "label": "profile_update"}
{"text": "請提供正式報價與價格明細", "label": "biz_quote"}
{"text": "收到", "label": "other"}
{"text": "重設密碼失敗", "label": "tech_support"}
{"text": "修改聯絡 email", "label": "profile_update"}
{"text": "修改聯絡 email", "label": "profile_update"}
{"text": "報價單可否提供最新版", "label": "biz_quote"}
{"text": "退款流程怎麼申請", "label": "policy_qa"}
{"text": "無法登入需要協助", "label": "tech_support"}
{"text": "物流延遲多日 非常不滿", "label": "complaint"}
{"text": "物流延遲多日 非常不滿", "label": "complaint"}
{"text": "退款流程怎麼申請", "label": "policy_qa"}
{"text": "系統錯誤請協助", "label": "tech_support"}
{"text": "退款流程怎麼申請", "label": "policy_qa"}
{"text": "更新客戶資料", "label": "profile_update"}
{"text": "你好 想了解更多", "label": "other"}
{"text": "收到", "label": "other"}
{"text": "想了解退款機制與使用限制", "label": "policy_qa"}
{"text": "系統錯誤請協助", "label": "tech_support"}
{"text": "請更新地址與電話", "label": "profile_update"}
{"text": "對服務品質感到失望", "label": "complaint"}
{"text": "對服務品質感到失望", "label": "complaint"}
{"text": "對服務品質感到失望", "label": "complaint"}
{"text": "請更新地址與電話", "label": "profile_update"}
{"text": "物流延遲多日 非常不滿", "label": "complaint"}
{"text": "物流延遲多日 非常不滿", "label": "complaint"}
{"text": "體驗很糟糕", "label": "complaint"}
{"text": "想了解退款機制與使用限制", "label": "policy_qa"}
{"text": "變更發票抬頭資訊", "label": "profile_update"}
{"text": "更新客戶資料", "label": "profile_update"}
{"text": "退款流程怎麼申請", "label": "policy_qa"}
```

### models/intent/data/val.jsonl
```
{"text": "可否安排方案與費用", "label": "biz_quote"}
{"text": "修改聯絡 email", "label": "profile_update"}
{"text": "物流延遲多日 非常不滿", "label": "complaint"}
{"text": "報價單可否提供最新版", "label": "biz_quote"}
{"text": "物流延遲多日 非常不滿", "label": "complaint"}
{"text": "報價單可否提供最新版", "label": "biz_quote"}
{"text": "無法登入需要協助", "label": "tech_support"}
{"text": "合約條款與限制", "label": "policy_qa"}
{"text": "修改聯絡 email", "label": "profile_update"}
{"text": "嗨", "label": "other"}
{"text": "重設密碼失敗", "label": "tech_support"}
{"text": "你好 想了解更多", "label": "other"}
{"text": "想了解退款機制與使用限制", "label": "policy_qa"}
{"text": "體驗很糟糕", "label": "complaint"}
{"text": "嗨", "label": "other"}
{"text": "請更新地址與電話", "label": "profile_update"}
{"text": "退款流程怎麼申請", "label": "policy_qa"}
{"text": "重設密碼失敗", "label": "tech_support"}
```

### models/intent/serving.py
```python
from __future__ import annotations
from typing import Dict, Any, List
import re

from . import train as intent_train

_SYNONYMS: Dict[str, List[str]] = {
    "biz_quote": ["sales", "quote"],
    "tech_support": ["support", "tech_support"],
    "profile_update": ["profile_update", "update_info"],
    "policy_qa": ["policy", "qa"],
    "complaint": ["complaint"],
    "other": ["other"],
}

def _tokenize(text: str) -> List[str]:
    lower = text.lower()
    zh = re.findall(r"[\u4e00-\u9fff]", lower)
    en = re.findall(r"[a-z0-9]+", lower)
    return zh + en

def predict(text: str) -> Dict[str, Any]:
    toks = _tokenize(text)
    rules = intent_train.load()
    label_scores: Dict[str, int] = {}
    for label in rules["labels"]:
        kws = rules["label_keywords"].get(label, [])
        hits = sum(1 for k in kws if any(k in t for t in toks))
        label_scores[label] = hits

    # 取分數最高的類別（平手時讓 biz_quote 優先，符合「合作/報價」常見文本）
    best = max(label_scores.items(), key=lambda kv: (kv[1], kv[0] == "biz_quote"))[0]
    return {
        "labels": _SYNONYMS.get(best, ["other"]),
        "raw": {"label": best, "scores": label_scores},
    }

```

### models/intent/serving_ml.py
```python
from __future__ import annotations
from pathlib import Path
from typing import Dict, Any, List
import json, joblib

ROOT = Path(__file__).resolve().parents[1]
ART  = ROOT/"intent"/"artifacts_ml"
_vec = _clf = _labels = None

def available() -> bool:
    return (ART/"vectorizer.joblib").exists() and (ART/"model.joblib").exists() and (ART/"labels.json").exists()

def _lazy_load():
    global _vec, _clf, _labels
    if _vec is None:
        _vec = joblib.load(ART/"vectorizer.joblib")
    if _clf is None:
        _clf = joblib.load(ART/"model.joblib")
    if _labels is None:
        _labels = json.loads((ART/"labels.json").read_text(encoding="utf-8"))

def predict(text: str) -> Dict[str, Any]:
    _lazy_load()
    Xv = _vec.transform([text])
    prob = getattr(_clf, "predict_proba", None)
    if prob:
        p = prob(Xv)[0]
        top = int(p.argmax())
    else:
        top = int(_clf.predict(Xv)[0])
        p = None
    label = _labels[top] if top < len(_labels) else "other"
    intents = [label]
    if label == 