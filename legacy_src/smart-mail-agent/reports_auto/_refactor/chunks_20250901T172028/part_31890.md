l_online.py
    7407  fee84d118ba64e36ce254d3357fd9f5921c683101ebe6ca9a03ecfd7b3523ed0  tests/models/conftest.py
    1371  2eaf1097824db794b6de9a211ad44e6125fd04b8c90ffb6f3d0a5b99fcd6c3c1  tests/models/test_cov_boost_ml.py
    5568  4973372fdbc3294b0c21684c0a12bf3b39e6653359844a1d24ab66b30966fa51  tests/models/test_cov_pad.py
     443  3e8d3255719e15d18f72d55648e54321c0ee4c074e49778acff8e824e03148df  tests/models/test_intent_training_serving.py
     587  c5b1ffc34d6f04b4fbca95233673d56e372d900e321583991525d39ad2b8bc94  tests/models/test_rag_build_and_query.py
     348  1f039bb30bf8092fdb09dda9747794ac1f1aea492c3e57cb6b493ab48a9a9b18  tests/models/test_spam_training_serving.py
    1103  db2515a99e24d4c59c2ec3ffaaff68b02e79da9c97dde4504d01f6719a66dbe9  tests/pro/test_actions_end2end.py
    1348  c9b57bbc58665d489c3a37b38b2b5d52c923e99fb2054155f418e066937296bb  tests/pro/test_actions_executor_enterprise_more.py
    1426  93e639fdce5537c967a4da7236a4eeffe432dd093288dadc5a04adecf1057436  tests/pro/test_actions_playbook_plan.py
     183  81001a952e844d96382929b67ce39aeecede3c279417b56a6d6d05f568581957  tests/pro/test_actions_router_gating.py
     393  e0ff692bcf411ff7f4cb5e37c234b22028539b71c32015bee901f04177e03b7c  tests/pro/test_actions_router_more.py
     987  95984055bc47fd09201a0945d9a33158e0a669531ca44bb18e73001032d29c10  tests/pro/test_actions_write_json_more.py
     596  1bb3a89ee4ba107c3e75ce8a782445e6ccc58ee2b7e6a2bcd7003f3324b55a8a  tests/pro/test_actions_write_json_more2.py
     341  3e7612e85ed564735008987ced5b42873f40d3b07f4ab167c46c004c4b1f4e69  tests/pro/test_actions_write_json_path.py
    1130  ea703e390e747cb96806f60abe128a11be30dc3818740f3ddf7e9efb349f97fe  tests/pro/test_config_loader_smoke_pro.py
    3548  2fe2428590e33ac476faa5a436a14ad9de23c7c4d17fcf3e60f181193d0c8492  tests/pro/test_cov_pad_more.py
    1148  5d187eb725ecd566112f190cce6711f4ec540d4bc4b70560f7c0e98dad51632b  tests/pro/test_end2end_categories.py
    1114  1edcc6ec394f2cbf3ac81b5683a1b858eeacb26f1acbc5d7dc2b4653aa36d8ab  tests/pro/test_file_classifier_smoke_pro.py
     269  b8c819612df5f3d9776172f56af6dc2ad72742199e3ab320a0593e32ec59ea91  tests/pro/test_json_safe.py
    1350  4edfa6720ed41c45107ac9f1abe71d9851d51f93efbf8837e65922555d57b371  tests/pro/test_json_safe_pro.py
    3624  76a12b3b75926a78736d5d3174cd77de8933cb78ac4736fcde1d8f91d0e7bf1a  tests/pro/test_mail_io_unit.py
     834  d70d5cb0ae10bf98bb6733a54e35ac8076e0208b101827bc8d41668829e67361  tests/pro/test_mailguard_enterprise_more.py
    1165  66c0d60ff09428c254b1df9cc32c0af36b75c6629103de10b81e8b60688b90e9  tests/pro/test_mailguard_gating.py
     924  c739950dcaff4af6aa7baf57caf4b76bac452e5f284b02daa12eed766ab2c1fa  tests/pro/test_mailguard_layers.py
     342  96654860b334e0dfb74684ace9e3f8f1243cff54fd4066a1bea8e7cc176a3aed  tests/pro/test_mailguard_variants.py
     801  609ee29339bfb8d776d93eafa07b74ff1634fc3282a90288652bd3305507b6b4  tests/pro/test_main_allow_online_smoke.py
     965  c382f0a41f993c2dbb2424ca014c972483eb0754503d379eb064e794f318d687  tests/pro/test_main_cli_more.py
    1080  95f06d9aaf4e48866c5f3762646b9ac3bdc07e3d159476f5796e2893ce73a879  tests/pro/test_main_contracts.py
     670  f27715e4c3287fcc79e6924abdf826f81250fa94f71a654c723d5b1d42a3db54  tests/pro/test_main_contracts_pro.py
    1752  5b8682bb3854d20cd7bf49a837647788fe80f9e76612abf82a9539b9e7b8bb30  tests/pro/test_main_gating_enterprise_more.py
     706  6b9785462654f21c1d8e44e6c1f00715f6d69106377f62de2320c9a347f3e07c  tests/pro/test_main_unknown_stdout.py
     837  491a0e2ba7428ee1ef602855213e224b2937e6e0e253c5b6c114451b24d4f6fe  tests/pro/test_nlp_llm_smoke_pro.py
     408  9bba6f4a5eca9422d39319f14574733192e573b539516ec0d369466d5fefe612  tests/pro/test_ocr_smoke_pro.py
     382  f74bb1354a3d0be8b1e80525294b98a738a1fcb7e6f2dadb7830e09ff40dba49  tests/pro/test_profile_update_diff_both.py
     980  ad19be0928b0d8ace9ec7414e5b267d610ae0492be654c8d23402709d54fb670  tests/pro/test_router_categories_enterprise_more.py
     326  654efd08cc3b6282ad03a806174b95e8c8891af84d15a89b872f75e91c0461a8  tests/pro/test_router_fallback_and_context.py
     227  c95618c5e929b2b10ae83ecd37050b72c8c063ae3257e2d94162912709aa848c  tests/pro/test_scraper_offline.py
     480  16e80ee08d579bf9b4d3e85fbf9790094fd1748cc41af1bf6c6497b64b56c723  tests/sma_core/test_jsonlog_extras.py
     975  5aa7565368a15c602ae999f7c6014d3a1e9caf0b708eeebfe18af702163a198e  tests/unit/test_log_writer_db_smoke.py
     531  75658b6687f0cd751deb5c61159bd2704ac13f003386296dae02b6786718de2c  tests/unit/test_logger_utils_smoke.py
     913  566fafe7f4d7eadfd62fc3ecf37d830afff69f1e2f4444cb0b9951f917b54d04  tests/unit/test_pdf_safe_extra.py
     606  c90274f4a3b5bd8b51f39cdb0a3d2f0a9a7d16cf482380ebe83751452b8e85f9  tests/unit/test_sma_types_normalize_extra.py
     122  ef71191741b32f32d1391b6d38ac76c45da00c84c5a96024a02c4c3b99050628  tests_smoke/test_imports_smoke.py
    1528  cee38cbb001796c63d006daa85be2589a73d908c7585d075f703cf865a0e1141  tests_smoke/test_pipeline_smoke.py
      28  a35bb2c3c37e9259a16690935637badf6a217ab52ab3c3a00bfc8766a59f49ad  tmp_ap02_smoke/in.txt
     166  45661aced76238cc31400803e948670de2fe848ea8ec25f7f328a45c7707894f  tmp_ap02_smoke/out.json
       4  ad0ae5ea5b905495886d47ae2cc726beaa50b58842de114f6dd7352074985df1  tmp_attachments/quote.txt
   12288  39957c395a804fb236bf9086cbab2a0f75801f7db457ce3775ace49b54c8c092  tmp_stats.db
     364  d760f03f9e17448981b3d04a918bdae38832b7f0713ea0841a207ad0f5519aea  tools/bootstrap.sh
     979  345b10ff834c4172f81bb85878f9e86693d05805223689765d5be80f78671e82  tools/ci-guard.sh
    1663  753951cffc4a0cc16b2d4b3236a66d5f02e0eadcc1832d5d35febb9d595179d4  tools/dev-check.sh
    7618  2ee5426aed090de1585e7af8b65d29d1b11806aa6019f52584467b76163a2f82  tools/dump_repo_into_10parts.py
     750  c00cbf67f134c7acc003625f260bcc8c2bb2bcbc89626fc1f2193ef56c74b299  tools/env.sh
    5589  04a61f5a7ec95cc059d76d31066e771d78aed5fa9f1c8b3a55c2b1d127142511  tools/fix_all_lints.py
    6340  40feb8314e1e7da7be257be156e45dd9d3752438f48d698afa23c1be7262e280  tools/fix_all_lints_v2.py
    1276  2de3411a1294f90f6147a5d6417dc25eec8ab7c99d5ac6b568e3710ace2083f0  tools/run_actions_matrix.py
    3130  0e810c02845e25402b3447972e17b4bdb19d3aba53123285d53c291b3b200797  tools/safe_refactor.py
     137  ae31fb499120e199357184fddea41789712077c11fee4856f454be190298d4a9  tox.ini
    4487  26620e8b2557db503fae65501f575bfe1e08024fafdd657eca89544e97c01dd6  unshadow_and_fix.sh
    6703  cc311197648b60aa7989cb8fde47322d65d94e6a1469deb123377023a9f2a358  update_quotation_shim_v2.sh
     665  616a4713f8b1456f575cb9c8f76deb29b123cc14189979cbfb37c1501d93ef61  utils/__init__.py
     127  a714667dcdf64adfd5471b881ed0ea55f6da47ff757df717d0265fd52e0b28b2  utils/log_writer.py
     133  e4338b861a911b4cf894616e2b727473b8ece8d4b76624f2bf9772c8f08b7618  utils/logger.py
    1311  8b029becf2078c62713a4954e4de83e34d496f35d3583ac04aedf79b48346e0e  utils/mailer.py

```

## reports_handcrafted/test_report.txt  
```text
                precision    recall  f1-score   support

     biz_quote      1.000     1.000     1.000         4
  tech_support      0.750     1.000     0.857         3
     policy_qa      1.000     1.000     1.000         2
profile_update      1.000     1.000     1.000         2
     complaint      1.000     0.500     0.667         2
         other      1.000     1.000     1.000         2

      accuracy                          0.933        15
     macro avg      0.958     0.917     0.921        15
  weighted avg      0.950     0.933     0.927        15

```

## reports_split/test_report.txt  
```text
                precision    recall  f1-score   support

     biz_quote      0.000     0.000     0.000       0.0
  tech_support      0.000     0.000     0.000       0.0
     policy_qa      0.000     0.000     0.000       0.0
profile_update      0.000     0.000     0.000       0.0
     complaint      0.000     0.000     0.000       0.0
         other      0.000     0.000     0.000      10.0

      accuracy                          0.000      10.0
     macro avg      0.000     0.000     0.000      10.0
  weighted avg      0.000     0.000     0.000      10.0

```

## samples/in.txt  
```text
合作與退款測試

```

## scripts/_apply_patch_boost.sh  
```bash
#!/usr/bin/env bash
# _apply_patch_boost.sh — 新增安全測試與覆蓋率腳本（不安裝、不創建 venv）
set -Eeuo pipefail
trap 'ec=$?; echo "[ERROR] 失敗於第 $LINENO 行（exit=$ec）" >&2' ERR

ROOT="${SMA_ROOT:-$HOME/projects/smart-mail-agent}"
[ -d "$ROOT/.git" ] && [ -d "$ROOT/src" ] || { echo "[FAIL] 非有效專案根：$ROOT"; exit 96; }
cd "$ROOT"

# 只用既有 Python
if [ -x .venv/bin/python ]; then PY=.venv/bin/python
elif [ -x "$HOME/.venv/sma/bin/python" ]; then PY="$HOME/.venv/sma/bin/python"
else PY="$(command -v python3 || command -v python || true)"
fi
[ -n "$PY" ] || { echo "[FAIL] 找不到 python"; exit 98; }

export OFFLINE=1 PYTHONNOUSERSITE=1 PYTHONPATH="$PWD/src:$PWD"
echo "[INFO] ROOT=$ROOT"
echo "[INFO] PY=$("$PY" -V 2>&1)"
echo "[INFO] OFFLINE=$OFFLINE"
echo "[INFO] PYTHONPATH=$PYTHONPATH"

mkdir -p tests/boost scripts

# 測試 1：兩個 CLI 入口的 --help（只印用法，無副作用）
cat > tests/boost/test_cli_help.py <<'PY'
import os, sys, subprocess, pathlib
ROOT = pathlib.Path(__file__).resolve().parents[2]
PYEXE = sys.executable
def _run_help(relpath: str):
    p = ROOT / relpath
    if not p.exists():
        return None
    env = os.environ.copy()
    env.setdefault("OFFLINE", "1")
    env["PYTHONPATH"] = f"{ROOT/'src'}:{ROOT}"
    cp = subprocess.run([PYEXE, str(p), "--help"], cwd=str(ROOT), env=env,
                        capture_output=True, text=True)
    out = (cp.stdout or "") + (cp.stderr or "")
    assert cp.returncode == 0, f"help failed: {relpath}\nstdout={cp.stdout}\nstderr={cp.stderr}"
    assert "usage" in out.lower(), f"no usage in help: {relpath}\n{out}"
    return out
def test_help_routing_entry():
    _run_help("src/smart_mail_agent/routing/run_action_handler.py")
def test_help_legacy_entry():
    _run_help("src/run_action_handler.py")
PY

# 測試 2：importlib 健檢（避免再誤判）
cat > tests/boost/test_importlib_sanity.py <<'PY'
def test_importlib_has_util():
    import importlib
    assert hasattr(importlib, "util")
PY

# 覆蓋率腳本（僅用現有環境執行 pytest；無副作用）
cat > scripts/boost_coverage_now.sh <<'BASH'
#!/usr/bin/env bash
set -Eeuo pipefail
ROOT="${SMA_ROOT:-$HOME/projects/smart-mail-agent}"
[ -d "$ROOT/.git" ] && [ -d "$ROOT/src" ] || { echo "[FAIL] 非有效專案根：$ROOT"; exit 2; }
cd "$ROOT"
if [ -x .venv/bin/python ]; then PY=.venv/bin/python
elif [ -x "$HOME/.venv/sma/bin/python" ]; then PY="$HOME/.venv/sma/bin/python"
else PY="$(command -v python3 || command -v python || true)"
fi
[ -n "$PY" ] || { echo "[FAIL] 找不到 python"; exit 3; }
export OFFLINE=1 PYTHONNOUSERSITE=1 PYTHONPATH="$PWD/src:$PWD"
echo "[INFO] pytest + coverage"
"$PY" -m pytest -q --maxfail=1 --disable-warnings \
  --cov=src --cov=modules --cov=smart_mail_agent \
  --cov-report=term-missing --cov-report=xml:coverage.xml
# 若有 genbadge 才產生徽章
"$PY" - <<'PY' || exit 0
import runpy
try:
    runpy.run_module("genbadge", run_name="__main__")
except Exception:
    raise SystemExit(0)
PY
if [ -f coverage.xml ]; then
  mkdir -p badges
  "$PY" -m genbadge coverage -i coverage.xml -o badges/coverage.svg || true
  echo "[INFO] 嘗試更新 badges/coverage.svg（若缺 genbadge 將略過）"
fi
echo "[DONE] 覆蓋率流程完成"
BASH
chmod +x scripts/boost_coverage_now.sh

# 僅驗證新增測試是否可跑（不跑整倉）
if "$PY" - <<'PY' >/dev/null 2>&1; then
try:
    import pytest  # noqa
    print("[CHECK] pytest 已安裝")
except Exception:
    raise SystemExit(1)
PY
then
  echo "[INFO] 驗證新增測試（tests/boost）"
  "$PY" -m pytest -q tests/boost -k "cli_help or importlib_sanity"
  echo "[NEXT] 如需完整覆蓋率，請執行：scripts/boost_coverage_now.sh"
else
  echo "[NEXT] 當前環境未安裝 pytest；已完成檔案新增，可待環境就緒後執行 scripts/boost_coverage_now.sh"
fi

echo "[DONE] 補丁完成"
exit 0

```

## scripts/ci_local.sh  
```bash
#!/usr/bin/env bash
set -Eeuo pipefail
ROOT="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
cd "$ROOT"
[ -x .venv/bin/python ] && . .venv/bin/activate || true
export PYTHONPATH=.
: "${COV_UNDER:=95}"
pytest --cov-fail-under="$COV_UNDER"
coverage-badge -o assets/badges/coverage.svg -f
echo "Done. Badge at assets/badges/coverage.svg"

```

## scripts/hotfix_stdout_and_import.sh  
```bash
#!/usr/bin/env bash
set -Eeuo pipefail
set -o errtrace
trap 'echo "[FAIL] line=$LINENO cmd=$BASH_COMMAND code=$?" >&2' ERR

log(){ printf '%s\n' "$*"; }
fail(){ printf '[FAIL] %s\n' "$*" >&2; exit 96; }

TS="$(date +%Y%m%dT%H%M%S)"
MAIN="src/ai_rpa/main.py"
[[ -f "$MAIN" ]] || fail "缺少 $MAIN"
cp -f "$MAIN" "$MAIN.bak.$TS"
log "[BACKUP] $MAIN -> $MAIN.bak.$TS"

python3 - "$MAIN" <<'PY'
from pathlib import Path
import re, sys

p = Path(sys.argv[1])
s = p.read_text(encoding="utf-8")

changed = False

# (1) 修正 NameError：將 if output_path: 統一換成 getattr(ns, 'output', None)
s2 = re.sub(r'\bif\s+output_path\b', "if getattr(ns, 'output', None)", s)
if s2 != s:
    s = s2
    changed = True

# (2) 先移除所有先前誤插的 print(payload)，避免重複輸出與縮排錯
s2 = re.sub(r'(?m)^\s*print\(\s*payload\s*\)\s*\n', '', s)
if s2 != s:
    s = s2
    changed = True

# (3) 在「payload = json.dumps(out, ensure_ascii=False)」當行之後、同一縮排層級，插入唯一的 print(payload)
pat = re.compile(r'(?m)^(\s*)payload\s*=\s*json\s*\.\s*dumps\s*\(\s*out\s*,\s*ensure_ascii\s*=\s*False\s*\)\s*$')

def _inject_print_once(text: str) -> str:
    m = pat.search(text)
    if not m:
        return text  # 找不到就不動，避免破壞；之後也不會報縮排錯
    indent = m.group(1)
    # 檢查接下來是否已有 print(payload)
    tail = text[m.end(): m.end()+200]
    if re.search(r'(?m)^\s*print\(\s*payload\s*\)\s*$', tail):
        return text  # 已存在
    return text[:m.end()] + f"\n{indent}print(payload)" + text[m.end():]

s2 = _inject_print_once(s)
if s2 != s:
    s = s2
    changed = True

# (4) 避免空的 else 導致縮排錯：如果 else: 之後緊接著 comment/空行/另一個 if，就補一個 pass
s2 = re.sub(r'(\n\s*else:\s*\n)(\s*)(?=(#.*\n|\s*\n|\s*if\b))', r'\1\2pass  # keep else non-empty\n', s)
if s2 != s:
    s = s2
    changed = True

if changed:
    p.write_text(s, encoding="utf-8")
    print("[PATCH] main.py: 修正 output 判斷 + 單一 stdout 輸出完成")
else:
    print("[SKIP] main.py 無需修補")
PY

# (5) 修 shim：若存在，確保使用相對匯入
SHIM="src/smart_mail_agent/spam/inference_classifier.py"
if [[ -f "$SHIM" ]]; then
  cp -f "$SHIM" "$SHIM.bak.$TS"
  python3 - "$SHIM" <<'PY'
from pathlib import Path
p = Path(__import__('sys').argv[1])
s = p.read_text(encoding='utf-8')
s2 = s.replace(
    "from src.smart_mail_agent.inference_classifier import (",
    "from ..inference_classifier import ("
)
if s2 != s:
    p.write_text(s2, encoding='utf-8')
    print("[PATCH] 修正 shim 匯入路徑")
else:
    print("[SKIP] shim 無需修補")
PY
fi

# (6) 最小煙囪：主程式必須吐出 JSON 到 stdout
mkdir -p samples
printf "合作與退款測試\n" > samples/in.txt
export OFFLINE="${OFFLINE:-1}"
out="$(python -m ai_rpa.main --tasks "nlp,mailguard,actions" --input-path samples/in.txt --dry-run)"
head -c 200 <<<"$out"; echo
[[ -n "$out" ]] || fail "未見 stdout JSON"
log "[OK] hotfix 完成"

```

## scripts/model_doctor.py  
```python
#!/usr/bin/env python3
from __future__ import annotations
import json, joblib, sklearn, sys
from pathlib import Path
from sklearn.pipeline import Pipeline, make_pipeline

PKL_IN  = Path("artifacts_prod/text_lr_platt.pkl")
PKL_OUT = Path("artifacts_prod/model_pipeline.pkl")
META    = Path("artifacts_prod/model_meta.json")
THRJS   = Path("artifacts_prod/ens_thresholds.json")

def to_pipeline(obj):
    if hasattr(obj, "predict_proba"):   # 已是可用 pipeline/estimator
        return obj, "as-is"
    if isinstance(obj, dict):
        # 常見：{'vect': TfidfVectorizer, 'cal': CalibratedClassifierCV}
        if "vect" in obj and "cal" in obj:
            return make_pipeline(obj["vect"], obj["cal"]), "rebuild(vect+cal)"
        if "pipeline" in obj and hasattr(obj["pipeline"], "predict_proba"):
            return obj["pipeline"], "use(dict['pipeline'])"
        # 退路：找第一個能 predict_proba 的
        for k,v in obj.items():
            if hasattr(v, "predict_proba"):
                return v, f"use(dict['{k}'])"
    raise TypeError("Cannot build a predict_proba pipeline from pkl")

def meta_of(pipe):
    meta={"sklearn": sklearn.__version__}
    try:
        vect = pipe.named_steps.get("tfidf", pipe.named_steps.get("vect"))
        if vect:
            meta["vectorizer"]={"class": type(vect).__name__, "ngram_range": getattr(vect,"ngram_range",None),
                                "analyzer": getattr(vect,"analyzer",None), "min_df": getattr(vect,"min_df",None),
                                "max_df": getattr(vect,"max_df",None)}
    except: pass
    try:
        cal = pipe.named_steps.get("cal")
        if cal:
            meta["calibrator"]=type(cal).__name__
            be = getattr(cal, "base_estimator", None) or getattr(cal, "estimator", None)
            if be is None and hasattr(cal, "calibrated_classifiers_"):
                try: be = cal.calibrated_classifiers_[0].base_estimator
                except: pass
            if be is not None:
                meta["base_estimator"]=type(be).__name__
                coef = getattr(be, "coef_", None)
                if coef is not None: meta["coef_shape"]=getattr(coef, "shape", None)
    except: pass
    if THRJS.exists():
        try: meta["threshold"]=json.loads(THRJS.read_text())["threshold"]
        except: pass
    return meta

obj = joblib.load(PKL_IN)
pipe, how = to_pipeline(obj)
# 煙霧測試（避免一維/二維 shape 錯誤）
_ = pipe.predict_proba(["Subject: smoke\nbody"])[:,1]
joblib.dump(pipe, PKL_OUT)
META.write_text(json.dumps({"built_via": how, **meta_of(pipe)}, ensure_ascii=False, indent=2))
print("[OK] normalized pipeline ->", PKL_OUT)
print(META.read_text())

```

## scripts/run_spam_drift.py  
```python
#!/usr/bin/env python3
# 檔案位置：scripts/run_spam_drift.py
# 用途：生成漂移報告
from smart_mail_agent.spam.train.drift_report import drift_report
import argparse
if __name__ == "__main__":
    ap = argparse.ArgumentParser()
    ap.add_argument("--data", required=True, help="未標註或新批次 JSONL")
    ap.add_argument("--rules", default=".sma_tools/spam_rules.yml")
    ap.add_argument("--vocab", default="artifacts/spam_vocab.json")
    ap.add_argument("--out", default="reports_auto")
    args = ap.parse_args()
    drift_report(args.data, args.rules, args.vocab, args.out)

```

## scripts/sma_data_factory_chat_v6.py  
```python
#!/usr/bin/env python3
from __future__ import annotations
import json, random, re, hashlib, argparse
from pathlib import Path
from datetime import date
from collections import defaultdict

# ---------- helpers ----------
def ngrams(s,n=3): return [s[i:i+n] for i in range(max(0,len(s)-n+1))]
def normalize(s):
    s=s.lower(); s=re.sub(r'\s+',' ',s); s=re.sub(r'\d','#',s); s=re.sub(r'[^\w\u4e00-\u9fff]+',' ',s); return s.strip()
def simhash(tokens):
    v=[0]*64
    for t in tokens:
        h=int(hashlib.md5(t.encode('utf-8')).hexdigest(),16)
        for i in range(64): v[i]+= 1 if (h>>i)&1 else -1
    f=0
    for i in range(64):
        if v[i]>0: f|=(1<<i)
    return f
def hamming(a,b): return (a^b).bit_count()
def jaccard(a,b):
    A=set(a); B=set(b)
    return 0.0 if not A or not B else len(A&B)/len(A|B)

# ---------- lexicons ----------
CO=["acme","megacorp","globalcorp","contoso","citypower","ispnet","healthcare","utilityco","anycompany","techfirm","xyzinc","infotech","shopfast","smartcloud","telecomco","safebank","fintrust"]
HAM_DOM=[f"{c}.com" for c in CO]+["company.com","service.com","support.com","billing.com","hr.com","admin.com","it.com","sales.com","example.com","example.org","example.net","bank.com","cloud.com","corp.tw","corp.com.tw"]
SUSP_TLD=["zip","xyz","top","click","link","quest","gq","cf","ml","work","rest"]
PHISH_BASE=[