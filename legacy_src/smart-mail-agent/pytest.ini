[pytest]
addopts = -q -m "not online"
markers =
    online: tests that require network/SMTP/LLM
    slow:   slow tests
testpaths =
    tests_smoke
    tests
filterwarnings =
    ignore::DeprecationWarning
