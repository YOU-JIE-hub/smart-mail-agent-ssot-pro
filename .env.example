# DB：PG 優先；留空則退回 SQLite (reports_auto/sma.sqlite3)
#SQLALCHEMY_URL=postgresql+psycopg2://user:pass@host:5432/dbname

# SMTP 假送控制（SEND_NOW=1 才真的出站）
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_SSL=1
SMTP_USER=
SMTP_PASS=
SEND_NOW=0

# IMAP（若要實機拉信）
IMAP_HOST=imap.example.com
IMAP_PORT=993
IMAP_SSL=1
IMAP_USER=
IMAP_PASS=
IMAP_MAILBOX=INBOX

# RAG
SMA_KB_INDEX_NAME=kb
