#!/usr/bin/env bash
set -Eeuo pipefail
# 統一 E2E 入口：目前串接 mail 流程；後續可擴充其他任務
ROOT="${SMA_ROOT:-$HOME/projects/smart-mail-agent_ssot}"
cd "$ROOT"
: "${SMA_EML_DIR:?SMA_EML_DIR 未設定（請指向含 .eml 的資料夾）}"
bash scripts/sma_e2e_mail.sh "$SMA_EML_DIR"
